package com.optimization;
import javax.swing.*;
public class ReducedSpace extends JFrame{
	public ReducedSpace()
	{
		setSize(600,500);
		setTitle("Optimization Framework");
		setDefaultCloseOperation(EXIT_ON_CLOSE);
		setResizable(false);
		setLayout(null);
	}
}
